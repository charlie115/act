import React from 'react';

import { useGetKpWebsocketDataQuery } from 'redux/api/websocket';

export default function RealTimePriceChart() {
  // const { data } = useGetPriceWebsocketDataQuery({ maxEntry: 128 });
  // console.log('data: ', data);
  return <div>RealTimePriceChart</div>;
}
